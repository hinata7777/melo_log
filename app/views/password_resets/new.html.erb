<div class="min-h-[60vh] flex items-center justify-center p-6">
  <div class="w-full max-w-md bg-gray-800 text-white rounded-2xl p-6 shadow">
    <h1 class="text-2xl font-bold mb-4 text-center">パスワードリセット</h1>
    <p class="text-sm text-gray-300 mb-4 text-center">登録メールアドレスに再設定リンクを送ります</p>

    <%= form_with url: password_resets_path, method: :post, local: true do %>
      <label class="block text-sm mb-1">メールアドレス</label>
      <input type="email" name="email" required
             class="w-full rounded-lg bg-gray-900 border border-gray-700 px-3 py-2 mb-4" />
      <button class="w-full bg-purple-600 hover:bg-purple-500 text-white font-semibold py-2 rounded-lg">
        送信する
      </button>
    <% end %>
  </div>
</div>
