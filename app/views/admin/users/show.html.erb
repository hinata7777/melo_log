<%= form_with model: [:admin, @user], method: :patch, class: "space-x-2" do |f| %>
  <%= f.select :role, [["general","general"],["admin","admin"]], {}, class: "border rounded p-1" %>
  <%= f.submit "権限を更新", class: "px-4 py-2 bg-blue-600 text-white rounded-lg" %>
<% end %>